import type { ColorScheme } from "./types";

export const colorSchemes: Record<string, ColorScheme> = {
  blue: { primary: "#3b82f6", light: "#93c5fd", bg: "#eff6ff" },
  green: { primary: "#10b981", light: "#6ee7b7", bg: "#ecfdf5" },
  purple: { primary: "#8b5cf6", light: "#c4b5fd", bg: "#f5f3ff" },
  orange: { primary: "#f59e0b", light: "#fbbf24", bg: "#fffbeb" }
};